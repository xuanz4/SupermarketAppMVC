<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel='stylesheet' href='/css/styles.css'>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Login | Supermarket App</title>
</head>
<body class="bg-surface">
  <section class="page-section py-5">
    <div class="container">
      <div class="row g-4 align-items-center">
        <div class="col-12 col-lg-6">
          <div class="card border-0 shadow-sm h-100" style="background: linear-gradient(135deg, #f5f7ff, #f0f8ff);">
            <div class="card-body">
              <p class="badge bg-primary-subtle text-primary mb-3">Welcome back</p>
              <h1 class="fw-bold text-navy mb-3">Sign in and pick up where you left off.</h1>
              <p class="text-muted mb-4">Access your saved cart, track orders, and get back to shopping in seconds.</p>
              <ul class="list-unstyled text-muted small mb-0">
                <li class="mb-2">• Fast checkout with saved delivery details</li>
                <li class="mb-2">• View previous orders and repeat buys</li>
                <li>• Secure sessions with quick logout</li>
              </ul>
            </div>
          </div>
        </div>
        <div class="col-12 col-lg-6">
          <div class="card border-0 shadow-sm">
            <div class="card-body p-4">
              <h4 class="mb-2">Login</h4>
              <p class="text-muted mb-3">Use your email and password to continue.</p>

              <% if (errors && errors.length) { %>
                <div class="alert alert-danger">
                  <% errors.forEach(function(error) { %>
                    <p class="mb-1"><%= error %></p>
                  <% }); %>
                </div>
              <% } %>

              <% if (messages && messages.length) { %>
                <div class="alert alert-success">
                  <% messages.forEach(function(message) { %>
                    <p class="mb-1"><%= message %></p>
                  <% }); %>
                </div>
              <% } %>

              <form action="/login" method="POST" class="mt-3">
                <div class="mb-3">
                  <label for="email" class="form-label">Email address</label>
                  <input type="email" id="email" name="email" class="form-control" placeholder="you@example.com" required>
                </div>
                <div class="mb-4">
                  <label for="password" class="form-label">Password</label>
                  <input type="password" id="password" name="password" class="form-control" placeholder="Enter your password" required>
                  <div class="form-check mt-2">
                    <input class="form-check-input" type="checkbox" id="toggleLoginPassword" data-toggle-password data-target="password">
                    <label class="form-check-label" for="toggleLoginPassword">Show password</label>
                  </div>
                </div>
                <button type="submit" class="btn btn-primary w-100">Login</button>
              </form>
              <p class="text-center text-muted mt-3 mb-0">New here? <a href="/register">Create an account</a></p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <footer class="footer">
    <div class="container">
      <small>&copy; <%= new Date().getFullYear() %> Supermarket App</small>
    </div>
  </footer>
  <script>
    document.addEventListener('DOMContentLoaded', function () {
      document.querySelectorAll('[data-toggle-password]').forEach(function (toggle) {
        var target = document.getElementById(toggle.dataset.target);
        if (!target) {
          return;
        }
        toggle.addEventListener('change', function () {
          target.type = toggle.checked ? 'text' : 'password';
        });
      });
    });
  </script>
</body>
</html>






